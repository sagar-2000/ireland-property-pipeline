name: 'ireland_property'
version: '1.0.0'
config-version: 2

profile: 'ireland_property'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ireland_property:
    staging:
      +materialized: view
    marts:
      +materialized: table
